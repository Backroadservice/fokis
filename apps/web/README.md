# Fokis Web Player Skeleton (Vite + React + TS) v1

最小のローカル `<video>` 再生 + View 適用（Pan/Zoom/Rotate/Opacity）デモ。後で YouTube/TikTok Facade に差し替え可能。

## 使い方
```
npm i
npm run dev
# ブラウザで http://localhost:5173
```
